import "./Spaces.css";

const BCheckerSpace: React.FC = () => {
  const render = (
    <div className="utilitySpace">
      <h2>BChecker (WIP)</h2>
      <h3>Generate your branch name</h3>
      <input type="text" />
    </div>
  );

  return render;
};

export default BCheckerSpace;
